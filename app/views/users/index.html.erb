<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
    <%= image_tag avatar_url(user) %>
      <%= link_to user.username, user %>
    </li>
  <% end %>
</ul>
